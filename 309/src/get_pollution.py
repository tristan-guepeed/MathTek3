#!/usr/bin/env python3

from src.get_file import get_file

def get_pollution(argv):
    fileContent = get_file(argv[2])
    n = int(argv[1])
    x = float(argv[3])
    y = float(argv[4])

    print("0.00")
    return (0)
